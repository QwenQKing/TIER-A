from __future__ import annotations
THEME_TYPES_6 = ['policy_shock', 'tech_wave', 'supply_chain_stress', 'sector_rotation', 'black_swan_spillover', 'macro_cycle', 'other']
THEME_NAME_PROMPT = 'The following set of corporate event episodes were clustered as a potential SECTOR/POLICY THEME — a wave that affected multiple stocks within a 90-day window.\n\nConstituent episodes (one per line, format: "[date | stock | episode_type] summary"):\n{episodes_block}\n\nMember stocks: {stocks}\nIndustries (already deduplicated, "Unknown" filtered out): {industries}\n\nTasks:\n  1. Name the theme in ≤8 words (e.g. "EV subsidy phase-out impact on battery chain").\n  2. Describe the underlying driver in ≤2 sentences (what is causing the cross-stock co-movement).\n  3. Decide if this is a real theme or just a clustering artifact.\n\nReturn JSON ONLY (no markdown):\n{{"is_theme": true | false,\n  "name": "<≤8 words>",\n  "description": "<≤2 sentences>",\n  "confidence": float in 0-1}}'
THEME_CLASSIFY_PROMPT = 'Classify this cross-stock theme into EXACTLY ONE of 7 types (the 7th is "other").\n\nTheme: {theme_name}\nDescription: {theme_desc}\nMember stocks: {stocks}\nSample episodes:\n{sample_episodes}\n\nTypes:\n  - policy_shock          : policy/regulatory drives multi-firm co-movement (subsidies, antitrust, sector ban)\n  - tech_wave             : technology adoption or new product category (AI, EVs, solid-state batteries)\n  - supply_chain_stress   : upstream/downstream transmission (chip shortage, raw material spike)\n  - sector_rotation       : capital flow between sectors\n  - black_swan_spillover  : single event contagion across peers\n  - macro_cycle           : rates/FX/cycle-driven cross-industry reaction\n  - other                 : theme is real but fits none above\n\nReturn JSON ONLY:\n{{"type": "<one of 7 values>", "confidence": float in 0-1}}'